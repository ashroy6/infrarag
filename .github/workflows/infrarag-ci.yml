# name: InfraRAG CI

on:
  workflow_dispatch:

# jobs:
#   backend-checks:
#     name: Backend syntax and import checks
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install backend dependencies
#         working-directory: backend
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Compile backend Python files
#         run: |
#           python -m compileall backend/app

#       - name: Import critical backend modules
#         run: |
#           PYTHONPATH=backend python - <<'PY'
#           import app.main
#           import app.router
#           import app.rag_orchestrator
#           import app.retrieve
#           import app.reranker
#           import app.metadata_db
#           print("Backend imports OK")
#           PY

#   frontend-checks:
#     name: Frontend static checks
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Check frontend files exist
#         run: |
#           test -f frontend/index.html
#           test -f frontend/admin.html

#       - name: Check index has InfraRAG app shell
#         run: |
#           grep -q "InfraRAG" frontend/index.html
#           grep -q "conversationList" frontend/index.html
#           grep -q "openCitation" frontend/index.html

#   docker-build:
#     name: Docker build check
#     runs-on: ubuntu-latest
#     needs:
#       - backend-checks
#       - frontend-checks

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Build backend image
#         run: |
#           docker compose build backend

#   compose-smoke:
#     name: Docker Compose smoke test
#     runs-on: ubuntu-latest
#     needs:
#       - docker-build

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Start core services
#         run: |
#           docker compose up -d qdrant ollama backend
#           sleep 20

#       - name: Health check backend
#         run: |
#           curl -fsS http://localhost:8000/health

#       - name: Check root endpoint
#         run: |
#           curl -fsS http://localhost:8000/

#       - name: Stop services
#         if: always()
#         run: |
#           docker compose down
