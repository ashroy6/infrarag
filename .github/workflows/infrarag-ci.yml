name: InfraRAG CI

on:
  workflow_dispatch:

jobs:
  disabled:
    name: CI disabled
    runs-on: ubuntu-latest
    steps:
      - name: Confirm CI is manual only
        run: echo "Automatic CI is disabled. This workflow only runs manually."
