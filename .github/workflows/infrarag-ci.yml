name: InfraRAG CI

on:
  workflow_dispatch:

jobs:
  disabled:
    name: CI disabled
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual-only workflow
        run: echo "Automatic CI is disabled. This workflow only runs manually."
